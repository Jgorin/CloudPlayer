import React, { useState } from "react"
import { postUser } from "./fetches/UserFetches"

const Home = (props) => {
  
  return(
    <div className="background fullscreen">
      <div className="indexScreen">
        <p className="white large">Some description of the app here...</p>
      </div>
    </div>
  )
}

export default Home